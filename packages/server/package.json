{
  "name": "@bolo/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @bolo/client build && wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx scripts/smoke.ts",
    "test": "vitest"
  },
  "dependencies": {
    "@bolo/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250508.0",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0",
    "vitest": "^2",
    "wrangler": "^4.14.0"
  }
}
